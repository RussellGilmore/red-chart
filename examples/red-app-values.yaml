# Red App configuration
app:
  name: red-app
  version: "1.0.0"

namespace:
  name: red-app
  create: true

domain:
  apex: rag-space.com
  subdomain: red-app

gateway:
  name: red-app-gateway
  namespace: istio-system

content:
  html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>Welcome to Rag App Test Application!</title>
      <style>
        body {
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
          max-width: 800px;
          margin: 0 auto;
          padding: 20px;
          line-height: 1.6;
        }
        h1 {
          color: #e74c3c;
        }
        .message {
          padding: 20px;
          background-color: #f8f9fa;
          border-left: 4px solid #e74c3c;
          margin-bottom: 20px;
        }
      </style>
    </head>
    <body>
      <h1>Welcome to Rag App Test Application!</h1>
      <div class="message">
        <p>This is a thing hopefully running in k8s within istio?</p>
      </div>
      <p>This page is served by an Nginx pod running in Kubernetes.</p>
      <p>Deployed with Red Chart v{{ .Chart.Version }}</p>
    </body>
    </html>